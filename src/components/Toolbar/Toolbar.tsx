const Toolbar = () => {

    return (
        <nav className="navbar navbar-expand-lg navbar-dark bg-primary">
            <div className="container-fluid">
                <span className="navbar-brand">User management application</span>
            </div>
        </nav>
    );
};

export default Toolbar;